CC = clang
LINK = -lstdc++ -lSDL2 -lOpenGL -lGLEW -lGLU
DBG = -Wall

all: build ;

targets = ./src/*.cpp ./src/*.h

build: $(targets) ;
	$(CC) $(DBG) $(targets) -o ./bin/ $(LINK)

clean: main.o ;
	rm -rf ./bin/*

run: build ;
	./bin/main.o
